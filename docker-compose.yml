version: '3.8'
services:
  train:
    build:
      context: .
      dockerfile: Dockerfile.train
    volumes:
      - ./train/data:/usr/src/data
      - ./new_trained_models:/usr/src/new_trained_models
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - 8000:8000
